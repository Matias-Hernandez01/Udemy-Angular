let skills = ['Bash', 'Counter', 'Healing', true];

interface character {
  name: string;
  hp: number;
  skills: string[];
}

const strider: character = {
  name: 'Matias',
  hp: 100,
  skills: ['Bash', 'Counter'],
};
